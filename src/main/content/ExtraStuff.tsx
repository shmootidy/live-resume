import ErrorSkull from '../../SharedComponents/ErrorSkull'
import LoadingSquares from '../../SharedComponents/LoadingSquares'

// this is just to show off things that aren't fully projects, but are fun
export default function ExtraStuff() {
  return (
    <>
      <LoadingSquares />
      <ErrorSkull />
    </>
  )
}
